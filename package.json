{
  "name": "hibari",
  "version": "0.0.0",
  "private": true,
  "description": "Capability-aware datastore compatibility layer.",
  "workspaces": [
    "packages/core",
    "packages/kintone",
    "packages/prisma",
    "packages/runtime-http"
  ],
  "scripts": {
    "build": "npm run build --workspace @hibari/core && npm run build --workspace @hibari/kintone && npm run build --workspace @hibari/prisma && npm run build --workspace @hibari/runtime-http",
    "check": "npm run check --workspace @hibari/core && npm run check --workspace @hibari/kintone && npm run check --workspace @hibari/prisma && npm run check --workspace @hibari/runtime-http",
    "test": "npm run test --workspace @hibari/core && npm run test --workspace @hibari/kintone && npm run test --workspace @hibari/prisma && npm run test --workspace @hibari/runtime-http && npm run test:integration",
    "test:integration": "node --import tsx --test test/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
