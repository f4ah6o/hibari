{
  "name": "hibari",
  "version": "0.0.0",
  "private": true,
  "description": "Capability-aware datastore compatibility layer.",
  "workspaces": [
    "packages/core",
    "packages/kintone"
  ],
  "scripts": {
    "build": "npm run build --workspace @hibari/core && npm run build --workspace @hibari/kintone",
    "check": "npm run check --workspace @hibari/core && npm run check --workspace @hibari/kintone",
    "test": "npm run test --workspace @hibari/core && npm run test --workspace @hibari/kintone"
  },
  "engines": {
    "node": ">=20"
  }
}
